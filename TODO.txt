Merge both building instance and unit instance module into one. They are pretty much the same right now, there is no point of them being seperate

Re organize non unit and non building modules

Shorten the client script by splitting parts of it up into smaller modules
